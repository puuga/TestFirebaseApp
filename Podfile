# Uncomment this line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'TestFirebaseApp' do

  # Pods for TestFirebaseApp
  pod 'Firebase'
  pod 'Firebase/Crash'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'

  target 'TestFirebaseAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestFirebaseAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
